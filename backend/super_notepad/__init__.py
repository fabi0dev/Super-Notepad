"""Super-Notepad — app de notas standalone (backend).

Extraído do app de Super-Notepad do Super Note: mesmo motor de armazenamento (SQLite),
mesmas rotas HTTP e o mesmo editor no frontend. Aqui os dados vivem num banco
dedicado (``notes.db``) sob a home do app (``~/.super-notepad`` por padrão).
"""

__version__ = "0.1.0"
